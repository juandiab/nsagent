NSAGENT_ENCRYPTION_KEY=replace_with_generated_fernet_key
JWT_SECRET_KEY=replace_with_random_secret
# Bootstrap admin — set by installer; leave blank when running against an existing DB
ADMIN_USERNAME=
ADMIN_PASSWORD=
MONGO_URI=mongodb://mongodb:27017
MONGO_DB=netscaler_copilot
MCP_SERVER_URL=http://mcp-server:8001

# SMTP for password reset emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_ADDRESS=
SMTP_USE_TLS=true
SMTP_USE_SSL=false
PASSWORD_RESET_LOG_CODES=false
